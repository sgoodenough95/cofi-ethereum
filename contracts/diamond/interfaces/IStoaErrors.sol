// SPDX-License-Identifier: MIT
pragma solidity 0.8.17;

interface IStoaErrors {

    // /// @notice Indicates if a claim operation failed because the requested amount is larger than the backing reserve.
    // ///
    // /// @param  asset   The backing asset to claim.
    // /// @param  amount  The current backing amount.
    // error InvalidReserveSurplus(address asset, uint256 amount);
}